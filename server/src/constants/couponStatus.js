/**
 * Centralized coupon status definitions.
 *
 * Prevents magic strings, typos, and makes future
 * status extensions easy (EXPIRED, DISABLED, etc).
 */
export const COUPON_STATUS = {
  ACTIVE: "ACTIVE",
  USED: "USED",
};
